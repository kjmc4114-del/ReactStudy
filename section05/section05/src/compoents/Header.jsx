
// const Header = () => {} <-- 화살표 함수로도 컴포넌트를 만들 수 있다. 모듈화를 위해서 컴포넌트별로 파일을 작성한다.
function Header() { // 컴포넌트는 대문자로 시작해야한다. 
  return (
    <header>
      <h1>Header</h1>
    </header>
  )
}

export default Header; 